# data, cache, trained artifacts, logs — regenerated by the scripts, never committed
out/
*.lz4
*_events.jsonl

# real configs hold an account address / live params; commit *.example.yaml instead
live_engine_config.yaml
spcx_leadlag_bot_config.yaml
jto_breakout_bot_config.yaml
grid_bot_live_ondo.yaml
grid_bot_config.yaml

# secrets
.env
*.pem
*.key

# python
__pycache__/
*.pyc
.venv/
venv/

# os / editor
.DS_Store
*.swp
